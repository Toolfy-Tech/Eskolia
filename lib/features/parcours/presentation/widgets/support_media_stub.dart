import 'dart:typed_data';

import 'package:flutter/widgets.dart';

String createBlobUrl(Uint8List bytes, String mimeType) => '';
void revokeBlobUrl(String url) {}
Widget buildPdfViewer(String blobUrl, String viewId) => const SizedBox.shrink();
Widget buildVideoViewer(String blobUrl, String viewId) => const SizedBox.shrink();
